#include<iostream>
#include<string>
#include<vector>
#include<stdexcept>
#include"video.h"
using namespace std;

//Implementierung der Klasse Video



        : string title, int length, Platform platforms;

        if (!platform)
            throw Platform::YouTube;

        if (!title)
            throw("runtime_error");

        if (lange < 5)
            throw("runtime_error")
